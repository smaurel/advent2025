import type { DaySolution } from "../../types";

/**
 * Solution pour le jour 4 de l'Advent of Code 2025
 *
 * Remplacez les implémentations de part1 et part2 par votre vraie logique
 */
const solution: DaySolution = {
  dayNumber: 4,

  part1: (input: string): string | number => {
    // TODO: Implémentez la solution de la partie 1
    return `Résultat partie 1 pour ${input.split("\n").length} lignes`;
  },

  part2: (input: string): string | number => {
    // TODO: Implémentez la solution de la partie 2
    return `Résultat partie 2 pour ${input.length} caractères`;
  },
};

export default solution;
